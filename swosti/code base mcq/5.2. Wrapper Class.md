**Wrapper Class কী? (Java)** — সহজ বাংলায়, ইন্টারভিউ-রেডি ব্যাখ্যা 👇

---

## 🔹 Wrapper Class কী?

Java-তে **Wrapper Class** হলো এমন একটি **class**, যা কোনো **primitive data type**-কে **object** আকারে wrap (মোড়ানো) করে।

👉 সহজভাবে:

> **Primitive → Object** বানানোর জন্যই Wrapper Class।

---

## 🔹 কেন Wrapper Class দরকার?

Java-তে কিছু জায়গায় **object ছাড়া কাজ করা যায় না**—যেমন:

* **Collections** (`List`, `Set`, `Map`)
* **Generics**
* **Frameworks** (Spring, Hibernate)
* **null value দরকার হলে**

Primitive (`int`, `double`…) এসব জায়গায় কাজ করে না।
তাই Wrapper Class লাগে।

---

## 🔹 Primitive vs Wrapper (টেবিল)

| Primitive | Wrapper Class |
| --------- | ------------- |
| `int`     | `Integer`     |
| `long`    | `Long`        |
| `double`  | `Double`      |
| `float`   | `Float`       |
| `char`    | `Character`   |
| `boolean` | `Boolean`     |
| `byte`    | `Byte`        |
| `short`   | `Short`       |

---

## 🔹 উদাহরণ

### ❌ Primitive দিয়ে Collection (ভুল)

```java
List<int> list = new ArrayList<>(); // ❌ Error
```

### ✅ Wrapper দিয়ে Collection (সঠিক)

```java
List<Integer> list = new ArrayList<>();
list.add(10);
```

---

## 🔹 Autoboxing & Unboxing

### Autoboxing (Primitive → Wrapper)

```java
int a = 10;
Integer b = a;   // Java নিজে থেকেই wrap করে
```

### Unboxing (Wrapper → Primitive)

```java
Integer x = 20;
int y = x;       // object → primitive
```

---

## 🔹 `null` বিষয়টা গুরুত্বপূর্ণ

```java
int a = null;        // ❌ সম্ভব না
Integer b = null;   // ✅ সম্ভব
```

⚠️ কিন্তু সাবধান:

```java
Integer b = null;
int x = b; // ❌ NullPointerException
```

---

## 🔹 Wrapper Class এর সুবিধা

✔ Object হিসেবে ব্যবহার করা যায়
✔ Collection-এ রাখা যায়
✔ `null` রাখা যায়
✔ অনেক utility method আছে

উদাহরণ:

```java
Integer.parseInt("123");   // String → int
Integer.valueOf("123");   // String → Integer
```

---

## 🔹 Wrapper Class এর অসুবিধা

❌ Primitive থেকে ধীর
❌ বেশি memory নেয়
❌ Unboxing করলে NPE হতে পারে

---

## 🔹 কখন কোনটা ব্যবহার করবো?

### ✅ `int` ব্যবহার করো যখন:

* Value অবশ্যই থাকবে
* Performance গুরুত্বপূর্ণ
* Loop, calculation

### ✅ `Integer` ব্যবহার করো যখন:

* `null` দরকার
* Collection / Framework
* Object প্রয়োজন

---

## 🧠 ইন্টারভিউ-তে বলার মতো এক লাইনের উত্তর

> **“Wrapper class primitive data type-কে object এ convert করে, যাতে আমরা collections, generics ও frameworks-এ ব্যবহার করতে পারি।”**

চাও তো আমি বুঝিয়ে দিতে পারি:

* `Integer cache`
* `== vs equals()`
* সব Wrapper MCQ
* Real interview traps
