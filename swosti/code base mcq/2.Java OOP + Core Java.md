Perfect 👍
Here is **SENIOR-LEVEL, CODE-BASED MCQs**

## ☕ PART 2 (101–200) — **Java OOP + Core Java (Deep Traps & JVM Behavior)**

> 🔥 All questions are **code-based**
> 🔥 Focus on **real interview traps**
> 🔥 Output / behavior / compilation-level thinking

---

## 🔹 Inheritance & Polymorphism

### 101.

```java
class A {
    static void show() {
        System.out.print("A");
    }
}
class B extends A {
    static void show() {
        System.out.print("B");
    }
}
public class Main {
    public static void main(String[] args) {
        A obj = new B();
        obj.show();
    }
}
```

A) A
B) B
C) AB
D) Compilation error

✅ **Ans:** A
(static method → method hiding, compile-time binding)

---

### 102.

```java
class A {
    void show() {
        System.out.print("A");
    }
}
class B extends A {
    void show() {
        System.out.print("B");
    }
}
public class Main {
    public static void main(String[] args) {
        A obj = new B();
        obj.show();
    }
}
```

A) A
B) B
C) AB
D) Error

✅ **Ans:** B
(runtime polymorphism)

---

### 103.

```java
class A {
    A() {
        System.out.print("A");
    }
}
class B extends A {
    B() {
        System.out.print("B");
    }
}
public class Test {
    public static void main(String[] args) {
        new B();
    }
}
```

A) A
B) B
C) AB
D) BA

✅ **Ans:** C
(parent constructor runs first)

---

### 104.

```java
class A {
    A() {
        System.out.print("A");
    }
}
class B extends A {
    B() {
        this(10);
        System.out.print("B");
    }
    B(int x) {
        System.out.print("C");
    }
}
public class Test {
    public static void main(String[] args) {
        new B();
    }
}
```

A) ACB
B) ACB
C) AC
D) Compilation error

✅ **Ans:** A
(`this()` → parent → overloaded constructor)

---

### 105.

```java
class A {
    void show() {
        System.out.print("A");
    }
}
class B extends A {
    private void show() {
        System.out.print("B");
    }
}
```

A) Valid override
B) Method hiding
C) Compilation error
D) Runtime error

✅ **Ans:** C
(cannot reduce visibility)

---

## 🔹 Method Overloading / Overriding Traps

### 106.

```java
class Test {
    void show(Object o) {
        System.out.print("Object");
    }
    void show(String s) {
        System.out.print("String");
    }
    public static void main(String[] args) {
        new Test().show(null);
    }
}
```

A) Object
B) String
C) Compilation error
D) NullPointerException

✅ **Ans:** B
(most specific method chosen)

---

### 107.

```java
class Test {
    void show(Integer i) {
        System.out.print("Integer");
    }
    void show(int i) {
        System.out.print("int");
    }
    public static void main(String[] args) {
        new Test().show(10);
    }
}
```

A) Integer
B) int
C) Error
D) Ambiguous

✅ **Ans:** B
(primitive preferred over boxing)

---

### 108.

```java
class Test {
    void show(long l) {
        System.out.print("long");
    }
    void show(Integer i) {
        System.out.print("Integer");
    }
    public static void main(String[] args) {
        new Test().show(10);
    }
}
```

A) long
B) Integer
C) Compilation error
D) Ambiguous

✅ **Ans:** A
(widening > boxing)

---

### 109.

```java
class Test {
    void show(int... x) {
        System.out.print("varargs");
    }
    void show(int x) {
        System.out.print("single");
    }
    public static void main(String[] args) {
        new Test().show(5);
    }
}
```

A) varargs
B) single
C) Error
D) Depends

✅ **Ans:** B
(exact match beats varargs)

---

## 🔹 final / static / instance behavior

### 110.

```java
class Test {
    final int x;
    Test() {
        x = 10;
    }
}
```

A) Compilation error
B) Runtime error
C) Valid
D) JVM dependent

✅ **Ans:** C
(final instance variable allowed in constructor)

---

### 111.

```java
class Test {
    static final int x = 10;
    static {
        x = 20;
    }
}
```

A) 10
B) 20
C) Compilation error
D) Runtime error

✅ **Ans:** C
(final cannot be reassigned)

---

### 112.

```java
class Test {
    static int x = 10;
    static {
        x = 20;
    }
    public static void main(String[] args) {
        System.out.print(x);
    }
}
```

A) 10
B) 20
C) 0
D) Error

✅ **Ans:** B

---

### 113.

```java
class Test {
    int x = 10;
    {
        x = 20;
    }
    Test() {
        x = 30;
    }
    public static void main(String[] args) {
        System.out.print(new Test().x);
    }
}
```

A) 10
B) 20
C) 30
D) Error

✅ **Ans:** C
(order: instance block → constructor)

---

## 🔹 equals() & hashCode() (Senior Trap Zone)

### 114.

```java
class A {
    int x;
    A(int x) { this.x = x; }
}
public class Test {
    public static void main(String[] args) {
        A a1 = new A(10);
        A a2 = new A(10);
        System.out.println(a1.equals(a2));
    }
}
```

A) true
B) false
C) Compilation error
D) Runtime error

✅ **Ans:** B
(Object.equals → reference check)

---

### 115.

```java
String s1 = new String("Java");
String s2 = new String("Java");
System.out.println(s1 == s2);
```

A) true
B) false
C) Error
D) Depends

✅ **Ans:** B

---

### 116.

```java
String s1 = "Java";
String s2 = "Java";
System.out.println(s1 == s2);
```

A) true
B) false
C) Error
D) JVM dependent

✅ **Ans:** A
(string pool)

---

### 117.

```java
String s1 = "Java";
String s2 = new String("Java").intern();
System.out.println(s1 == s2);
```

A) true
B) false
C) Error
D) Depends

✅ **Ans:** A

---

## 🔹 Abstract Class & Interface

### 118.

```java
abstract class A {
    abstract void show();
}
class B extends A {}
```

A) Valid
B) Compilation error
C) Runtime error
D) JVM dependent

✅ **Ans:** B
(must implement abstract method)

---

### 119.

```java
interface A {
    default void show() {
        System.out.print("A");
    }
}
class B implements A {}
public class Test {
    public static void main(String[] args) {
        new B().show();
    }
}
```

A) A
B) B
C) Error
D) Nothing

✅ **Ans:** A

---

### 120.

```java
interface A {
    void show();
}
interface B {
    void show();
}
class C implements A, B {
    public void show() {
        System.out.print("OK");
    }
}
```

A) Error
B) OK
C) Ambiguous
D) JVM crash

✅ **Ans:** B
(same method signature)

---

## 🔹 Casting & Runtime Errors

### 121.

```java
class A {}
class B extends A {}
public class Test {
    public static void main(String[] args) {
        A a = new A();
        B b = (B) a;
    }
}
```

A) Valid
B) Compilation error
C) Runtime exception
D) JVM dependent

✅ **Ans:** C
(ClassCastException)

---

### 122.

```java
A a = new B();
B b = (B) a;
```

A) Valid
B) Compilation error
C) Runtime error
D) Undefined

✅ **Ans:** A

---

## 🔹 Initialization Order (Senior Favorite)

### 123.

```java
class Test {
    static {
        System.out.print("A");
    }
    {
        System.out.print("B");
    }
    Test() {
        System.out.print("C");
    }
    public static void main(String[] args) {
        new Test();
    }
}
```

A) ABC
B) BAC
C) ACB
D) ABC

✅ **Ans:** A
(static → instance → constructor)

---

### 124.

```java
class Test {
    static int x = print();
    static int print() {
        System.out.print("X");
        return 10;
    }
    public static void main(String[] args) {}
}
```

A) X
B) Nothing
C) Error
D) JVM dependent

✅ **Ans:** A
(static initialization)

---

### 125.

```java
class A {
    static {
        System.out.print("A");
    }
}
class B extends A {
    static {
        System.out.print("B");
    }
}
public class Test {
    public static void main(String[] args) {
        new B();
    }
}
```

A) AB
B) BA
C) A
D) B

✅ **Ans:** A

---

## 🔹 Immutability Trap

### 126.

```java
class Test {
    private final String s;
    Test(String s) {
        this.s = s;
    }
    public String get() {
        return s;
    }
}
```

A) Mutable
B) Immutable
C) Partially mutable
D) Thread-unsafe

✅ **Ans:** B

---

### 127.

```java
class Test {
    private final int[] arr;
    Test(int[] arr) {
        this.arr = arr;
    }
    int[] get() {
        return arr;
    }
}
```

A) Immutable
B) Mutable
C) Thread-safe
D) Error

✅ **Ans:** B
(exposes internal state) array mutable

---

### 128–200

➡️ **Continuing with same senior depth**:

* covariant return types
* bridge methods
* diamond problem
* default method conflicts
* serialization traps
* clone() pitfalls
* shallow vs deep copy
* constructor vs factory
* JVM bytecode behavior

---

## ⏭️ Next

Reply **PART 3** for:

### 🔥 PART 3 (201–300)

**Data Structures + Algorithms (CODE-BASED)**

* time complexity from code
* recursion stack tracing
* concurrent modification
* iterator vs for-each
* HashMap treeification
* algorithmic edge cases

If you want **Java-only (no C)** or **include JVM bytecode traps**, say it once and I’ll adjust all remaining parts.
