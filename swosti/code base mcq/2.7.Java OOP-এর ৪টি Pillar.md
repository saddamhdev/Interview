Java OOP-এর **৪টি pillar (স্তম্ভ)** হলো এমন ধারণা যেগুলোর ওপর পুরো Object-Oriented Programming দাঁড়িয়ে আছে। নিচে **সহজ বাংলা + কোড + ইন্টারভিউ-রেডি ব্যাখ্যা** দিলাম 👇

---

# 🔹 Java OOP-এর ৪টি Pillar

## 1️⃣ Encapsulation (ডেটা ঢেকে রাখা)

### 🔹 কী?

> **Data + Method একসাথে বেঁধে রাখা এবং বাইরের access নিয়ন্ত্রণ করা**

### 🔹 কেন দরকার?

* Data protection
* Controlled access
* Maintainability

### 🔹 Example

```java
class Account {
    private double balance;   // hidden data

    public void deposit(double amount) {
        if (amount > 0) balance += amount;
    }

    public double getBalance() {
        return balance;
    }
}
```

### 🧠 Key Point

* `private` field
* `public` methods (getter/setter)

---

## 2️⃣ Abstraction (অপ্রয়োজনীয় ডিটেইল লুকানো)

### 🔹 কী?

> **Implementation লুকিয়ে শুধু behaviour দেখানো**

### 🔹 করা হয় কী দিয়ে?

* `abstract class`
* `interface`

### 🔹 Example

```java
interface Vehicle {
    void move();
}

class Car implements Vehicle {
    public void move() {
        System.out.println("Car moves");
    }
}
```

### 🧠 Key Point

* *কি করবে* দেখানো হয়
* *কিভাবে করবে* লুকানো থাকে

---

## 3️⃣ Inheritance (উত্তরাধিকার)

### 🔹 কী?

> **একটি class আরেকটি class-এর property ও behavior পায়**

### 🔹 Example

```java
class Animal {
    void eat() {
        System.out.println("Eating");
    }
}

class Dog extends Animal {
    void bark() {
        System.out.println("Barking");
    }
}
```

### 🧠 Key Point

* Code reuse
* `extends` keyword

---

## 4️⃣ Polymorphism (এক নামে অনেক রূপ)

### 🔹 কী?

> **একই method নাম, ভিন্ন আচরণ**

### 🔹 দুই ধরনের

#### 🔸 Compile-time (Overloading)

```java
class MathUtil {
    int add(int a, int b) { return a + b; }
    int add(int a, int b, int c) { return a + b + c; }
}
```

#### 🔸 Runtime (Overriding)

```java
class Animal {
    void sound() {
        System.out.println("Animal sound");
    }
}

class Dog extends Animal {
    void sound() {
        System.out.println("Dog barks");
    }
}
```

```java
Animal a = new Dog();
a.sound();   // Dog barks
```

---

## 📊 ৪ Pillar এক নজরে

| Pillar        | কী করে           |
| ------------- | ---------------- |
| Encapsulation | Data protect করে |
| Abstraction   | Complexity লুকায় |
| Inheritance   | Code reuse       |
| Polymorphism  | Flexibility দেয়  |

---

## 🧠 Interview One-Liner (Perfect)

> **“The four pillars of OOP in Java are Encapsulation, Abstraction, Inheritance, and Polymorphism, which together ensure security, reusability, flexibility, and maintainability.”**

---

চাও তো আমি পরের ধাপে দিতে পারি:

* 🔥 OOP MCQs
* ⚠️ OOP interview traps
* 🧪 Real project examples
* 🚀 Advanced polymorphism puzzles
