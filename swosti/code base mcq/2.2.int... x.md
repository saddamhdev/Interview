### **`int... x` কী? (Java Varargs — সহজ ও গভীর ব্যাখ্যা)**

---

## 🔹 `int... x` মানে কী?

`int... x` হলো **varargs (variable arguments)** —
মানে একটি method **যেকোনো সংখ্যক `int` value** নিতে পারে।

👉 সহজভাবে:

> **`int... x` ≡ `int[] x` (ভিতরে ভিতরে array)**

---

## 🔹 কেন Varargs দরকার?

আগে আমাদের আলাদা আলাদা overload লিখতে হতো:

```java
void sum(int a, int b) {}
void sum(int a, int b, int c) {}
void sum(int a, int b, int c, int d) {}
```

Varargs এ সব এক method-এই সম্ভব:

```java
void sum(int... x) {}
```

---

## 🔹 Basic Example

```java
static void print(int... x) {
    for (int i : x) {
        System.out.print(i + " ");
    }
}
```

### Call করা যায় এভাবে:

```java
print();            // no argument
print(1);           // 1
print(1, 2, 3);     // 1 2 3
```

---

## 🔹 ভিতরে আসলে কী হয়?

Compiler এটাকে **array বানিয়ে নেয়**:

```java
print(1, 2, 3);
```

⬇️ JVM internally করে:

```java
print(new int[]{1, 2, 3});
```

---

## 🔹 `int...` vs `int[]`

| Feature               | `int...` | `int[]` |
| --------------------- | -------- | ------- |
| Syntax                | Cleaner  | Verbose |
| Caller friendly       | ✅        | ❌       |
| Inside method         | Array    | Array   |
| Overloading ambiguity | ⚠️ Yes   | ❌       |

Inside method দুটোই একই:

```java
void m(int... x) { }
void m(int[] x) { }
```

---

## 🔹 Varargs rules (VERY IMPORTANT)

### ✅ Rule 1: Varargs must be **last parameter**

```java
void test(int a, int... b) {}   // ✅ OK
void test(int... a, int b) {}   // ❌ Compile error
```

---

### ✅ Rule 2: Only **ONE varargs** allowed

```java
void test(int... a, String... b) {} // ❌ Error
```

---

### ✅ Rule 3: Varargs can be **empty**

```java
test(); // valid → x.length = 0
```

---

## 🔹 Null trap ⚠️

```java
test(null);
```

👉 Ambiguous:

* Is `null` an `int[]`?
* Or something else?

Often leads to **compile-time error** if overloaded.

---

## 🔹 Overloading ambiguity (Interview trap)

```java
void test(int... x) {}
void test(int[] x) {}
```

❌ Compile-time error
(They are same after compilation)

---

## 🔹 Real-world use cases

* `System.out.printf(String format, Object... args)`
* `String.format(...)`
* Logging frameworks
* Utility methods

---

## 🧠 Interview One-Liner

> **“`int... x` is varargs, allowing a method to accept zero or more int values. Internally, it is treated as an int array.”**

---

## 🔥 Quick MCQ

```java
void m(int... x) {
    System.out.println(x.length);
}

m(1, 2, 3);
```

**Output?**
👉 `3`

---

If you want:

* ⚠️ Varargs + overloading traps
* 🧪 Bytecode view
* 🚀 Advanced Java MCQs
* 🔥 Real interview questions

Just tell me 👍
