চল **Java-তে `this` vs `super`** একদম **simple + clear** ভাবে বুঝি 👇
(এটা খুব common interview + real-code topic)

---

## 🔹 `this` কী?

> **`this` = current object (নিজের object)**

যখন তুমি **একই class-এর**:

* field
* method
* constructor

এসবকে refer করতে চাও, তখন `this` ব্যবহার করো।

### ✅ Example

```java
class Person {
    String name;

    Person(String name) {
        this.name = name; // field = parameter
    }

    void show() {
        System.out.println(this.name);
    }
}
```

📌 `this.name` → current object-এর `name`

---

## 🔹 `super` কী?

> **`super` = parent class object**

Inheritance থাকলে:

* parent class-এর field
* parent class-এর method
* parent constructor

access করতে `super` ব্যবহার করা হয়।

### ✅ Example

```java
class Animal {
    String type = "Animal";

    void sound() {
        System.out.println("Animal sound");
    }
}

class Dog extends Animal {
    String type = "Dog";

    void print() {
        System.out.println(type);        // Dog
        System.out.println(super.type);  // Animal
    }

    @Override
    void sound() {
        super.sound(); // calls Animal's sound
        System.out.println("Dog barks");
    }
}
```

---

## 🔁 `this` vs `super` (Side-by-Side)

| Feature               | `this`               | `super`             |
| --------------------- | -------------------- | ------------------- |
| Refers to             | Current class object | Parent class object |
| Access fields         | ✅ Current class      | ✅ Parent class      |
| Access methods        | ✅ Current class      | ✅ Parent class      |
| Call constructor      | `this()`             | `super()`           |
| Used with inheritance | ❌ Not required       | ✅ Required          |

---

## 🧠 Constructor Case (Very Important)

### 🔹 `this()` — same class constructor

```java
class A {
    A() {
        System.out.println("Default");
    }

    A(int x) {
        this(); // calls A()
        System.out.println(x);
    }
}
```

---

### 🔹 `super()` — parent class constructor

```java
class A {
    A() {
        System.out.println("Parent");
    }
}

class B extends A {
    B() {
        super(); // calls A()
        System.out.println("Child");
    }
}
```

📌 Rule:

* `this()` বা `super()`
* **constructor-এর প্রথম লাইনে থাকতে হবে**
* একসাথে দুটো ব্যবহার করা যাবে না ❌

---

## ❌ Common Mistakes

```java
this(); 
super(); // ❌ illegal together
```

```java
super.superMethod(); // ❌ no grandparent direct access
```

---

## 🎯 Interview One-liners

* **`this`** refers to the **current class instance**
* **`super`** refers to the **immediate parent class instance**
* Constructor-এ `super()` না লিখলেও Java **automatically first line-এ বসিয়ে দেয়**

---

## 🧠 Easy Memory Trick

> **this = আমি নিজে**
> **super = আমার বাবা**

---

যদি চাও, আমি next-এ দেখাতে পারি:

* `this` keyword real bug examples
* `super` with overriding vs hiding
* Why `super()` is implicit
* Tricky interview MCQs

বললেই বলো 😊
