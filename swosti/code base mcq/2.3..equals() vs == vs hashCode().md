নীচে **`.equals()` vs `==` vs `hashCode()`**—এই তিনটার **স্পষ্ট পার্থক্য**, **কখন কী ব্যবহার করবেন**, আর **ইন্টারভিউ ট্র্যাপ**—সব একসাথে, পরিষ্কারভাবে দিলাম 👇

---

## 1️⃣ `==` কী করে?

**`==` অপারেটর** দুইটা জিনিসের **reference (memory address)** একই কিনা চেক করে
(primitive হলে **value** চেক করে)

### 🔹 Primitive case

```java
int a = 10;
int b = 10;
System.out.println(a == b); // true (value comparison)
```

### 🔹 Object case

```java
Integer a = 128;
Integer b = 128;
System.out.println(a == b); // false (reference comparison)
```

👉 আলাদা object → আলাদা reference

---

## 2️⃣ `.equals()` কী করে?

**`.equals()` method** সাধারণত **value comparison** করে
(যদি class এটাকে override করে)

### 🔹 Example

```java
Integer a = 128;
Integer b = 128;

System.out.println(a.equals(b)); // true
```

✔ value একই → true

⚠️ কিন্তু!
যদি class `.equals()` override না করে, তাহলে default আচরণ = `==`

```java
class A {}
A x = new A();
A y = new A();
System.out.println(x.equals(y)); // false (same as ==)
```

---

## 3️⃣ `hashCode()` কী?

`hashCode()` একটা **integer number** রিটার্ন করে, যা object-এর **hash identity** বোঝায়।

👉 এটা সরাসরি comparison-এর জন্য নয়
👉 **Hash-based collections** (`HashMap`, `HashSet`) এর জন্য

```java
Integer a = 128;
Integer b = 128;

System.out.println(a.hashCode()); // same
System.out.println(b.hashCode()); // same
```

---

## 4️⃣ Golden Contract (VERY IMPORTANT)

### 🔥 `equals()` & `hashCode()` Contract

> যদি
> `a.equals(b) == true`
> তাহলে
> `a.hashCode() == b.hashCode()` **হতেই হবে**

❌ উল্টোটা বাধ্যতামূলক না

---

## 5️⃣ HashMap / HashSet এ কীভাবে কাজ করে?

### 🔹 `HashSet` example

```java
Set<String> set = new HashSet<>();
set.add(new String("java"));
set.add(new String("java"));

System.out.println(set.size()); // 1
```

### কেন?

1. `hashCode()` একই → same bucket
2. `equals()` true → duplicate discard

---

## 6️⃣ Custom Class example (COMMON INTERVIEW)

### ❌ ভুল (equals override, hashCode না)

```java
class Person {
    int id;

    public boolean equals(Object o) {
        return this.id == ((Person)o).id;
    }
}
```

👉 HashSet-এ bug হবে

---

### ✅ সঠিক

```java
class Person {
    int id;

    @Override
    public boolean equals(Object o) {
        if (this == o) return true;
        if (!(o instanceof Person)) return false;
        Person p = (Person) o;
        return id == p.id;
    }

    @Override
    public int hashCode() {
        return Objects.hash(id);
    }
}
```

---

## 7️⃣ Side-by-side Comparison

| Feature             | `==`      | `.equals()`     | `hashCode()` |
| ------------------- | --------- | --------------- | ------------ |
| Type                | Operator  | Method          | Method       |
| Compares            | Reference | Value (usually) | Hash value   |
| Override possible   | ❌ No      | ✅ Yes           | ✅ Yes        |
| Used in collections | ❌         | ✅               | ✅            |
| Primitive support   | ✅         | ❌               | ❌            |

---

## 8️⃣ Common Interview Traps ⚠️

### 🔹 Trap 1: String

```java
String a = "java";
String b = "java";
System.out.println(a == b);      // true (string pool)
System.out.println(a.equals(b)); // true
```

```java
String a = new String("java");
String b = new String("java");
System.out.println(a == b);      // false
System.out.println(a.equals(b)); // true
```

---

### 🔹 Trap 2: Null

```java
String s = null;
s.equals("a"); // ❌ NullPointerException
```

✅ Safe way:

```java
Objects.equals(s, "a"); // false
```

---

## 🧠 Interview One-Liner (Perfect)

> **“`==` compares references, `.equals()` compares logical equality, and `hashCode()` supports hashing; equal objects must have equal hash codes.”**

---

যদি চাও:

* 🔥 MCQs on equals/hashCode
* 🧪 JVM internals of hashing
* ⚠️ Real HashMap bugs
* 🚀 Coding interview questions

বললেই শুরু করবো 👍
