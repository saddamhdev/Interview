### ✅ Correct Answer: **B) Exclusive row lock**

---

## 🔒 `SELECT ... FOR UPDATE` — Effect Explained

```sql
SELECT * FROM orders WHERE id = 10 FOR UPDATE;
```

**Effect কী?**

➡️ এটি **selected rows-এর উপর exclusive row-level lock** নেয়।

---

## 🔍 What exactly happens?

* শুধু **যে row গুলো SELECT হয়েছে** সেগুলো lock হয়
* অন্য transaction:

  * ❌ `UPDATE` করতে পারবে না
  * ❌ `DELETE` করতে পারবে না
  * ⏳ wait করবে যতক্ষণ না current transaction `COMMIT / ROLLBACK` করে

---

## ❌ What it does NOT do

| Option         | Why wrong                   |
| -------------- | --------------------------- |
| A) Shared lock | `FOR SHARE` shared lock দেয় |
| C) Table lock  | Table-level lock নয়         |
| D) No lock     | Lock অবশ্যই নেয়             |

---

## 🧠 PostgreSQL Lock Types (Quick)

| Clause              | Lock Type             |
| ------------------- | --------------------- |
| `FOR UPDATE`        | 🔒 Exclusive row lock |
| `FOR SHARE`         | 👥 Shared row lock    |
| `FOR NO KEY UPDATE` | 🔒 Lighter exclusive  |
| `FOR KEY SHARE`     | 👥 Lighter shared     |

---

## 🎯 Interview One-liner

> **`SELECT ... FOR UPDATE` acquires an exclusive lock on the selected rows, preventing concurrent updates or deletes until the transaction ends.**

---

✔️ **Final Answer:** **B) Exclusive row lock**
