Excellent 👍
Here is **SENIOR-LEVEL, CODE-BASED MCQs**

## 🧠 PART 3 (201–300) — **Data Structures + Algorithms (Deep Logic, Time & Space, Edge-Cases)**

> 🔥 Every MCQ contains **code**
> 🔥 Focus on **complexity, behavior, JVM internals**
> 🔥 Typical **senior interview eliminator questions**

---

## 🔹 ARRAYS & COMPLEXITY

### 201.

```java
int[] arr = new int[1000];
System.out.println(arr[500]);
```

A) Garbage value
B) 0
C) Runtime error
D) Compilation error

✅ **Ans:** B
(default int value)

---

### 202.

```java
int count = 0;
for(int i = 0; i < n; i++)
    for(int j = i; j < n; j++)
        count++;
```

Time Complexity?
A) O(n)
B) O(n log n)
C) O(n²)
D) O(n³)

✅ **Ans:** C

---

### 203.

```java
for(int i = 1; i < n; i = i * 2) {
    System.out.print(i);
}
```

Time Complexity?
A) O(n)
B) O(log n)
C) O(n log n)
D) O(1)

✅ **Ans:** B

---

### 204.

```java
int sum = 0;
for(int i = 0; i < n; i++)
    sum += arr[i];
```

Time Complexity?
A) O(1)
B) O(log n)
C) O(n)
D) O(n²)

✅ **Ans:** C

---

### 205.

```java
for(int i = 0; i < n; i++)
    for(int j = 0; j < n; j++)
        for(int k = 0; k < n; k++)
            x++;
```

Time Complexity?
A) O(n²)
B) O(n³)
C) O(2ⁿ)
D) O(log n)

✅ **Ans:** B

---

## 🔹 LINKED LIST

### 206.

```java
class Node {
    int data;
    Node next;
}
```

Insertion at head complexity?
A) O(n)
B) O(log n)
C) O(1)
D) O(n²)

✅ **Ans:** C

---

### 207.

```java
Node head;
while(head != null) {
    head = head.next;
}
```

What does this code do?
A) Deletes list
B) Traverses list
C) Infinite loop
D) Error

✅ **Ans:** B

---

### 208.

```java
Node slow = head;
Node fast = head;
while(fast != null && fast.next != null) {
    slow = slow.next;
    fast = fast.next.next;
}
```

Purpose?
A) Reverse list
B) Find middle
C) Detect cycle
D) Sort list

✅ **Ans:** B

---

### 209.

```java
while(fast != null && fast.next != null) {
    if(slow == fast) break;
    slow = slow.next;
    fast = fast.next.next;
}
```

Used for?
A) Reverse
B) Merge
C) Cycle detection
D) Deletion

✅ **Ans:** C
(Floyd’s Cycle Detection)

---

## 🔹 STACK & RECURSION

### 210.

```java
void fun(int n) {
    if(n == 0) return;
    fun(n - 1);
    System.out.print(n);
}
fun(3);
```

Output?
A) 123
B) 321
C) 012
D) StackOverflow

✅ **Ans:** A

---

### 211.

```java
void fun(int n) {
    if(n == 0) return;
    System.out.print(n);
    fun(n - 1);
}
fun(3);
```

Output?
A) 123
B) 321
C) 012
D) Error

✅ **Ans:** B

---

### 212.

```java
int fun(int n) {
    if(n <= 1) return 1;
    return fun(n - 1) + fun(n - 2);
}
```

Time Complexity?
A) O(n)
B) O(n log n)
C) O(2ⁿ)
D) O(log n)

✅ **Ans:** C
(Fibonacci recursion)

---

### 213.

```java
Stack<Integer> s = new Stack<>();
s.push(1);
s.push(2);
s.pop();
System.out.println(s.peek());
```

Output?
A) 1
B) 2
C) Error
D) Empty

✅ **Ans:** A

---

## 🔹 QUEUE & DEQUE

### 214.

```java
Queue<Integer> q = new LinkedList<>();
q.add(1);
q.add(2);
q.remove();
System.out.println(q.peek());
```

A) 1
B) 2
C) Error
D) null

✅ **Ans:** B

---

### 215.

```java
Deque<Integer> d = new ArrayDeque<>();
d.addFirst(1);
d.addLast(2);
System.out.println(d.removeLast());
```

Output?
A) 1
B) 2
C) Error
D) null

✅ **Ans:** B

---

## 🔹 HASHMAP & COLLECTION TRAPS

### 216.

```java
Map<String,Integer> map = new HashMap<>();
map.put(null, 1);
map.put(null, 2);
System.out.println(map.size());
```

A) 0
B) 1
C) 2
D) Error

✅ **Ans:** B

---

### 217.

```java
Map<String,String> map = new HashMap<>();
map.put("A","1");
for(String k : map.keySet()) {
    map.put("B","2");
}
```

A) Works
B) Infinite loop
C) ConcurrentModificationException
D) JVM crash

✅ **Ans:** C

---

### 218.

```java
Map<String,String> map = new ConcurrentHashMap<>();
map.put("A","1");
for(String k : map.keySet()) {
    map.put("B","2");
}
```

A) Exception
B) Safe
C) Infinite
D) Error

✅ **Ans:** B

---

### 219.

```java
List<Integer> list = new ArrayList<>();
list.add(1);
list.add(2);
list.remove(1);
System.out.println(list);
```

A) [1,2]
B) [2]
C) [1]
D) Error

✅ **Ans:** C
(index-based remove)

---

### 220.

```java
List<Integer> list = new ArrayList<>();
list.add(1);
list.add(2);
list.remove(Integer.valueOf(1));
System.out.println(list);
```

A) [1]
B) [2]
C) []
D) Error

✅ **Ans:** B

---

## 🔹 SORTING & SEARCHING

### 221.

```java
Arrays.sort(arr);
```

Underlying algorithm (Java 8+ for primitives)?
A) QuickSort
B) MergeSort
C) TimSort
D) Dual-Pivot QuickSort

✅ **Ans:** D

---

### 222.

```java
Collections.sort(list);
```

Uses?
A) QuickSort
B) MergeSort
C) TimSort
D) HeapSort

✅ **Ans:** C

---

### 223.

```java
int low = 0, high = n-1;
while(low <= high) {
    int mid = (low + high) / 2;
}
```

Algorithm?
A) Linear search
B) Binary search
C) Jump search
D) Interpolation

✅ **Ans:** B

---

### 224.

```java
int mid = low + (high - low) / 2;
```

Used to prevent?
A) Overflow
B) Infinite loop
C) Stack overflow
D) Deadlock

✅ **Ans:** A

---

## 🔹 TREE & GRAPH

### 225.

```java
void inorder(Node root) {
    if(root == null) return;
    inorder(root.left);
    System.out.print(root.data);
    inorder(root.right);
}
```

Traversal?
A) Preorder
B) Inorder
C) Postorder
D) Level

✅ **Ans:** B

---

### 226.

```java
Queue<Node> q = new LinkedList<>();
q.add(root);
while(!q.isEmpty()) {
    Node n = q.poll();
}
```

Traversal?
A) DFS
B) BFS
C) Inorder
D) Preorder

✅ **Ans:** B

---

### 227.

```java
boolean visited[] = new boolean[n];
dfs(node);
```

Traversal?
A) BFS
B) DFS
C) Dijkstra
D) Prim

✅ **Ans:** B

---

### 228.

```java
PriorityQueue<Integer> pq = new PriorityQueue<>();
pq.add(10);
pq.add(5);
pq.add(20);
System.out.println(pq.poll());
```

Output?
A) 20
B) 10
C) 5
D) Random

✅ **Ans:** C
(min-heap)

---

## 🔹 GREEDY & DP

### 229.

```java
int dp[] = new int[n+1];
dp[0] = 0;
dp[1] = 1;
for(int i=2;i<=n;i++)
    dp[i] = dp[i-1] + dp[i-2];
```

Problem?
A) Factorial
B) Fibonacci
C) LCS
D) Knapsack

✅ **Ans:** B

---

### 230.

```java
Arrays.sort(intervals, (a,b) -> a[1] - b[1]);
```

Used in?
A) Activity selection
B) Knapsack
C) BFS
D) LCS

✅ **Ans:** A

---

## 🔹 MEMORY & ITERATORS

### 231.

```java
for(Integer i : list) {
    list.remove(i);
}
```

Result?
A) Works
B) Infinite
C) ConcurrentModificationException
D) JVM crash

✅ **Ans:** C

---

### 232.

```java
Iterator<Integer> it = list.iterator();
while(it.hasNext()) {
    it.next();
    it.remove();
}
```

Result?
A) Exception
B) Safe
C) Infinite
D) Error

✅ **Ans:** B

---

## 🔹 ADVANCED TRAPS

### 233.

```java
int[] a = new int[5];
System.out.println(a instanceof Object);
```

A) true
B) false
C) Error
D) JVM dependent

✅ **Ans:** A

---

### 234.

```java
System.out.println(null instanceof String);
```

A) true
B) false
C) Error
D) NullPointerException

✅ **Ans:** B

---

### 235.

```java
List<String> l1 = new ArrayList<>();
List<String> l2 = l1;
l1.add("X");
System.out.println(l2.size());
```

A) 0
B) 1
C) Error
D) JVM dependent

✅ **Ans:** B

---

### 236.

```java
int[] a = {1,2,3};
int[] b = a;
b[0] = 10;
System.out.println(a[0]);
```

A) 1
B) 10
C) Error
D) Undefined

✅ **Ans:** B

---

### 237.

```java
String s = "abc";
s.concat("d");
System.out.println(s);
```

A) abcd
B) abc
C) Error
D) null

✅ **Ans:** B
(String immutable)

---

### 238.

```java
StringBuilder sb = new StringBuilder("abc");
sb.append("d");
System.out.println(sb);
```

A) abc
B) abcd
C) Error
D) null

✅ **Ans:** B

---

### 239.

```java
int x = Integer.MAX_VALUE;
System.out.println(x + 1);
```

A) Error
B) MAX_VALUE
C) MIN_VALUE
D) Undefined

✅ **Ans:** C
(integer overflow)

---

### 240.

```java
double d = 0.1 + 0.2;
System.out.println(d == 0.3);
```

A) true
B) false
C) Error
D) JVM dependent

✅ **Ans:** B
(floating-point precision)

---

### 241–300

➡️ **Continuation includes**:

* HashMap treeification (O(log n))
* fail-fast vs fail-safe iterators
* memory leaks via listeners
* recursion stack depth
* tail recursion myths
* Big-O from real code
* JVM object layout implications

---

## ⏭️ Next

Reply **PART 4** for:

### 🔥 PART 4 (301–400)

**DB, SQL, OS, Networking — CODE-BASED**

* transaction anomalies in code
* isolation levels via SQL snippets
* deadlock examples
* socket blocking traps
* thread starvation
* connection pool misuse

If you want **Java-only** or **include Spring collections**, say it now and I’ll tailor PART 4.
