নিচে **Abstract Class vs Interface**—একদম **interview-ready**, পরিষ্কার বাংলা ব্যাখ্যা, **rules + examples + traps**সহ দিলাম 👇

---

# 🔹 Abstract Class & Interface (Java)

## 1️⃣ Abstract Class কী?

**Abstract class** এমন একটি class যেখানে:

* **abstract method** থাকতে পারে (body ছাড়া)
* **concrete method**ও থাকতে পারে (body সহ)
* Object তৈরি করা যায় না
* Inheritance (`extends`) এর জন্য ব্যবহৃত

```java
abstract class Animal {
    abstract void sound();   // no body

    void sleep() {            // concrete method
        System.out.println("Sleeping");
    }
}
```

---

## 2️⃣ Interface কী?

**Interface** হলো **pure contract / blueprint**:

* By default সব method **public abstract**
* Java 8+ এ `default` ও `static` method থাকতে পারে
* Fields সবসময় **public static final**
* `implements` keyword ব্যবহার করে

```java
interface Flyable {
    int MAX = 100;          // public static final

    void fly();             // public abstract
}
```

---

## 3️⃣ Key Differences (Table)

| Feature                 | Abstract Class      | Interface                         |
| ----------------------- | ------------------- | --------------------------------- |
| Keyword                 | `abstract`          | `interface`                       |
| Methods                 | abstract + concrete | abstract (default/static allowed) |
| Variables               | Any type            | `public static final` only        |
| Constructor             | ✅ Yes               | ❌ No                              |
| Multiple inheritance    | ❌ No                | ✅ Yes                             |
| Access modifiers        | Any                 | public only (methods)             |
| State (instance fields) | ✅ Can have          | ❌ Cannot                          |
| `extends / implements`  | `extends`           | `implements`                      |

---

## 4️⃣ Inheritance Example

### Abstract Class

```java
abstract class Vehicle {
    abstract void move();
}

class Car extends Vehicle {
    void move() {
        System.out.println("Car moves");
    }
}
```

---

### Interface

```java
interface Flyable {
    void fly();
}

class Bird implements Flyable {
    public void fly() {
        System.out.println("Bird flies");
    }
}
```

---

## 5️⃣ Multiple Inheritance (Major Difference)

### ❌ Abstract Class

```java
class A {}
class B {}
class C extends A, B {}  // ❌ Not allowed
```

---

### ✅ Interface

```java
interface A {}
interface B {}

class C implements A, B {}  // ✅ Allowed
```

---

## 6️⃣ Java 8+ Interface Enhancements

### Default Method

```java
interface I {
    default void show() {
        System.out.println("Default method");
    }
}
```

### Static Method

```java
interface I {
    static void test() {
        System.out.println("Static method");
    }
}
```

Call:

```java
I.test();
```

---

## 7️⃣ Diamond Problem (Interface solves it)

```java
interface A {
    default void show() {
        System.out.println("A");
    }
}

interface B {
    default void show() {
        System.out.println("B");
    }
}

class C implements A, B {
    public void show() {
        A.super.show(); // resolve conflict
    }
}
```

---

## 8️⃣ When to use what? (Interview GOLD)

### ✅ Use Abstract Class when:

* **IS-A relationship**
* Shared code + state
* Base class with fields
* Partial implementation

### ✅ Use Interface when:

* **CAN-DO behavior**
* Multiple inheritance needed
* Framework API / contract
* Loose coupling

---

## 9️⃣ Common Interview Traps ⚠️

### Trap 1: Constructor in Interface

```java
interface I {
    I() {}  // ❌ Compile error
}
```

---

### Trap 2: Method visibility

```java
interface I {
    void m();   // implicitly public
}

class A implements I {
    void m() {} // ❌ cannot reduce visibility
}
```

---

### Trap 3: Variables

```java
interface I {
    int x = 10;   // public static final
}
```

Cannot change `x`.

---

## 🧠 One-Line Interview Answer

> **“Abstract class provides partial abstraction with state, while interface defines a contract enabling multiple inheritance and loose coupling.”**

---

If you want:

* 🔥 Abstract vs Interface MCQs
* ⚠️ Java 8+ default method traps
* 🧪 Real Spring Boot usage
* 🚀 Senior interview puzzles
